export interface FollowedPlayer {
  playerId: string;
  name: string;
  teamAbbrev: string;
  sweaterNumber: string | number;
  positionCode: string;
  imageUrl: string;
  teamColor: string;
  teamName: string;
}
